# This file is placed in the Public Domain.
# doctest: +ELLIPSIS

"brokers"

"""
>>> from nixt.defines import Broker, Object
>>> b = Broker()
>>> o = Object()
>>> repr(o) # doctest: +ELLIPSIS
'<nixt.objects.Object object at ...'
>>> id = b.add(o)
>>> print(id) # doctest: +ELLIPSIS
<nixt.objects.Object object at ...>
>>> obj = b.get(id)
>>> repr(obj) == id
True

"""
